# Production Environment Variables
# This file is used when building for production (npm run build)
# Vercel will use these values during the build process

# API URL for production - points to deployed backend
REACT_APP_API_URL=https://mahindraservicesapi.vercel.app

# If you're deploying the frontend to a different domain than the backend,
# make sure CORS is enabled on the backend to allow requests from your frontend domain
